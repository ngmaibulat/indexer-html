// Define the structure of a document
export interface Document {
  id: string; // File name
  title: string; // <title> content
  heading: string; // <h1> content (optional)
  body: string; // <article> content
}
